server {
    listen 80;
    server_name ejemploweb.duckdns.org;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name ejemploweb.duckdns.org;

    ssl_certificate /etc/letsencrypt/live/ejemploweb.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ejemploweb.duckdns.org/privkey.pem;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ =404;
    }
}